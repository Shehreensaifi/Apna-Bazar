extends base

block links
    link(rel="stylesheet", href="/css/order.css")

block content
    div#title-wrapper
        h2.title Your Orders
    main
        .container
            -orders.forEach(order=>{
                .item
                    .status
                        h4=order.status
                    img(src=`${order.product.image}`, alt=`${order.product.name}`)
                    .detail
                        h3=order.product.name
                        button More
                    h2.price= `₹${order.product.price}`
            -});
            

